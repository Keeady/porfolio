[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.54",
]

[tool.vercel]
entrypoint = "main:app"
